# Template only — do NOT commit real values.
# Create the secret directly with kubectl (never apply this file with real credentials):
#
#   kubectl create secret generic contact-api-secret \
#     --namespace civpulse-dev \
#     --from-literal=DATABASE_URL='postgresql+asyncpg://contact_api:PASSWORD@postgresql.civpulse-infra.svc.cluster.local:5432/contact_api'
#
# Replace PASSWORD with the actual password chosen during database setup (Step 1).
