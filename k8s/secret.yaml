# Template only — do NOT commit real values.
# Generate base64 value: echo -n 'your-value' | base64
apiVersion: v1
kind: Secret
metadata:
  name: contact-api-secret
type: Opaque
data:
  DATABASE_URL: REPLACE_WITH_BASE64_ENCODED_DATABASE_URL
