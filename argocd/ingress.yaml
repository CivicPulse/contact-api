apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: argocd-server
  namespace: argocd
spec:
  entryPoints:
    - web
  routes:
    - kind: Rule
      match: Host(`<TAILSCALE_HOST>`) && PathPrefix(`/argocd`)
      services:
        - name: argocd-server
          port: 80
