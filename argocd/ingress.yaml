apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: argocd-server
  namespace: argocd
spec:
  entryPoints:
    - web
  routes:
    - kind: Rule
      match: Host(`thor.tailb56d83.ts.net`) && PathPrefix(`/argocd`)
      services:
        - name: argocd-server
          port: 80
